# Copia este archivo como .env y llena los valores

ANTHROPIC_API_KEY=sk-ant-...

# WooCommerce (solo necesario para sitios con woocommerce=true en sites/*.json,
# ej. peptidosysuplementos. Sitios sin WooCommerce como arcademotors no lo usan)
WC_STORE_URL=https://peptidosysuplementos.mx
WC_CONSUMER_KEY=ck_...
WC_CONSUMER_SECRET=cs_...

# Base de datos PostgreSQL (Railway la inyecta automaticamente en produccion;
# en local usa tu propia instancia)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# URLs de agentes hermanos (ya configuradas por default — cambiar solo si el URL cambia)
BLOG_AGENT_URL=https://agente-blogs-production.up.railway.app
SEO_AGENT_URL=https://web-production-3743c.up.railway.app

# NEXUS — centro de comando (opcional, deja vacio si no aplica)
NEXUS_URL=
NEXUS_KEY=

# Flask
FLASK_SECRET_KEY=cambia-esto-por-una-cadena-larga-aleatoria
